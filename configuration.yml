---
general:
  gcp: false
  logging_mode: terminal

logging:
  filename: log.txt

###
from pathlib import Path
from os import path, sep
repo_name = 'ci-util'
cwd_path = path.normpath(Path.cwd())
dir_structure = cwd_path.split(sep)
index_repo_name = dir_structure.index(repo_name)
project_home = Path(*dir_structure[:index_repo_name+1])
conf_file_location = Path.joinpath(project_home, 'configuration.yml')
print(cwd_path)
print(project_home)
print(conf_file_location)
###

